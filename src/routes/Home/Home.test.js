import React from 'react'
import { render, fireEvent, screen } from '@testing-library/react'
import Home from './Home'

test('test render Home', () => {
    // render(<Home />)

    // expect(screen.getByTestId('content-test-id')).toBeInTheDocument
})